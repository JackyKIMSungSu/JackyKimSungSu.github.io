# AI 부트캠프 MLOps
- 

## MLOps

### 03-04. [실습 1] 환경 관리 툴 실습
> Conda
- 환경 생성
  - conda create [env_name] python[=version]
  - conda activate [env_name]
  - conda deactivate
- 환경 백업
  - conda env export > [backup_env].yml
- 백업된 환경 생성(백업파일로 부터)
  - conda env create --name [env_name] -f [backup_env].yml
>virtualenv
- pip install virtualenv
- 환경만들기 : virtualenv myenv [-p /usr/bin/python3.8]
- 환경들어가기 : source myenv/bin/activate
- 환경나오기 : deactivate
- 

### 03-05. MLOps를 구현하기 위한 인프라와 도구 3 (Container 이론)
> Container란?
- 자체 파일 시스템을 가지고 있는 가벼운 가상환경
- 
- 독립된 실행 환경
- 경량화 : 전체 OS를 가상화하지 않고 필요한 컴포넌트만 포함
- 이미지 기반
> Container 주요 구성요소 & 필요성
- Image
  - Docker 이미지는 Dockerfile로 정의
- Registry
  - Container Image가 저장되고 공유되는 곳
  - Docker Hub, Google Container Registry
- Container Runtime
  - Docker, rkt등을 이용하여 Container Instance를 생성
- 필요성
  - 환경 일관성 제공
  - 자원 효율성 증가 : VM보다 적은 리소스를 사용, 자원 사용률 최적화
  - 배포 및 확장 용이성 : CI/CD 파이프라인과 잘 통합 빠른 개발 및 배포 가능
> Container 장단점
- 경량성 및 빠른 시작
- 일관된 환경
- 효율적인 자원 사용
- 격리와 분리
- 단점 : 보안취약성, 관리 복잡성
> Container 활용 사례
- 멀티 클라우드 및 하이브리드 클라우드 환경에서 활용
- 대규모 머신러닝 파이프라인 최적화 : Docker 컨테이너 사용
- 지속적 통합 및 배포(CI/CD)를 위한 활용 : Jenkins, Travis CI + Docker Container

### 03-06. [실습 2] Container 실습
> 실습 소개
- Docker 생성 단계
  - 1. Dockerfile 작성
  - 2. Dockerfile 내에 스크립트를 실행한다면, 스크립트 작성
  - 3. Docker 이미지 빌드
  - 4. Docker 컨테이너 실행
> 실습 환경 구축
- docker download : gist.github.com/franklinyu/
- mac용 Docker Desktop 4.24.2(2023-10-12) macOS 12 버전에서
- docker search nginx
- docker pull nginx : nginx다운로드
- docker run -it -d -p 8001:80 --name nginxserver nginx:latest &
- docker ps : docker 운영 중인 내용 확인
- docker stop [dockerrun_name or dockerrun_id]
- docker exec -it [dockerrun_id] bash : docker안으로 들어가기
  - cd /usr/share/.../html
- docker cp [대상파일] [dockerrun_name]:[복사할 파일_경로포함]

> docker regist활용
- vim Dockerfile
- 작업
- docker build -t [docker명]
- docker images
- docker run --name [이용명] [docker명]
- dockerhub에 올리기 위해
  - docker tag [이용명:latest] [dockerhub표시명]/[이용명:latest]
  - docker push [dockerhub표시명]/[이용명:latest]
- docker pull [dockerhub표시명]/[이용명:latest]