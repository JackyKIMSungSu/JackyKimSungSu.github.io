# AI 부트캠프 리눅스 실전 정복

## 리눅스 운영파트

### Ch.2 리눅스 쉘과 CLI 명령어
#### 기본 명령어
- 파일 내용 보기
  - cat
  - more
  - less
- 파일 링크(ln - link)
  - 하드링크 : ln hello.txt hellolink
  - 소프트링크(심볼릭 링크) : ln -s hello.txt. hellolink
- 파일 시스템 구조(inode 맛보기)
  - mode, owner, size, timestamp, direct blocks, indirect blocks 와 같은 파일 정보를 가지는 파일 시스템
- 파일 속성 보기
  - file
- 시스템 종료
  - reboot : 바로 적용
  - poweroff : 바로 적용
  - shutdown [OPTIONS] [TIME] : 주어진 시간에 종료(기본값 +1 - 1분후)
    - shutdown -P now : 바로 종료
    - shutdown -r now : 바로 재시작
- 도움말 : man
- 편집기 : vi, vim, nano

### 사용자와 권한 관련 명령어
- 계정 종류
  - root, userID
  - /etc/passwd, /etc/shadow, /etc/group
  - whoami, id
  - sudoer, sudo
- sudo
  - 슈퍼유저(superuser) 권한을 수행(do)
  - useradd -aG user1 sudo (Ubuntu)
  - useradd -aG user1 wheel (Amazon AMI)
- 파일의 특수 실행 권한(setuid, setgid, sticky bit)
  - chmod u+s filename
  - chmod g+s filename
  - chmod +t directoryname