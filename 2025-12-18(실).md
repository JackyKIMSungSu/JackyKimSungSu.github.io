# 프로젝트를 위한 협업 : Github
- 강사 : 최우영
 
# Start git


  
## github flow
- Local | Pull request 부터는 Remote에서


- 깃헙에서 이슈만들고
- 로컬에서 브랜치 만들고
- 브랜치에서 코드 작업하고
- 브랜치에서 커밋
- 브랜치에서 git push -u
  - remote에 github에 브랜치 만듬
  - git push -u origin feature/fizzbuzz
  - -u 옵션은 처음 원격 브랜치를 만들 때
- github에 feature/fizzbuzz 브랜치 나옴
- Pull request 실행
  - Add title 등
- 이후 검토 절차 진행(Code Review)
  - File changed 탭에서 
- Merge Pull Request 진행
- Delete branch 진행 => 완료
- 로컬에서는 최신화 안되어 있음(원격에서는 최신화됨)
  - git pull origin main 
  - or 
  - git fetch origin main > git merge FETCH_HEAD
- Issue 닫기

## github local<->remote conflict
> 우선 remote에서 merge pull request에서 에러 발생시
> local : branch에서 작업
- git fetch origin main
- git merge FETCH_HEAD
- 에러 발생
- 수정 후
- git add
- git commit
- git push origin {branchname}

## git stash 
> 작업 내용을 임시 저장
> git stash pop 저장된 내용을 돌려 놓음
> stash는 불완전한 작업내용을 임시 저장


## github flow with fork
- 영상 녹화본 나중에 참고
- 팀장, 팀원화면 다름  
> 팀장
- new organization 세팅
  - name, email 등 입력 skip
  - Invite member
  - 새 레포지토리 생성 : Team Repository
  - 로컬에 git clone
  - 기본 세팅 : .gitignore
  - 기본 대상 파일 만들어 줌 
    - touch main.py 등...
  - git add, commit, push
  - Issue 템플릿 : 
    - Setting > Feature > Template 작성
> 팀원
- Forked Repository 로 작업
- 이슈를 쓰고 꼭 작업
- fork 진행
- forked repository url 복사
- git clone {forked repository url}
- git remote add upstream {team repository url} : fetch & merge 위해 원격 팀 주소 등록
- 코딩 작업 후 git add, commit
- 이슈에 기록 남김
- git push -u origin {branchname}
- github에서 Pull request 진행
- 이 때, 이슈와 연결 > resolve #{이슈번호}
- 자세한 설명 첨부하여 작성

> 팀장 : 팀 레포지터리에서
- 코드리뷰 진행
- 여러 옵션이 있음
- merge & 

> 팀원들
- git fetch upstream main
