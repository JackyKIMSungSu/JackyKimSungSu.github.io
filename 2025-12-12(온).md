# AI 부트캠프 컴퓨터 공학

## Part 9. 네트워크

### Ch3. 네트워크 계층
> 06. IP 주소의 분류
>
> 07. 라우팅

### Ch.4 전송 계층
> 01. 포트
>
> 02. [실습]포트 확인하기
>
> 03. TCP와 UDP
>
> 04. [실습]TCP, UDP 패킷 분석
>
> 05. TCP 연결
- 연결 
  - 3 way hand-shake
  - A:[SYN]->B:[SYN, ACK]->A:[ACK]
- close : 
  - 4 times-close
  - A:[FIN,ACK]->B:[ACK]->B:[FIN,ACK]->A:[ACK]
  - 연결종료시 B는 [FIN,ACK] 송신시 바로
  - A는 [ACK] 송신 후 일정시간 경과 후 연결종료
> 06. TCP 상태
- 상태 목록
  - CLOSED 
  - LISTEN : 서버호스트는 항상 SYN 세크먼트를 기다리는 상태
  - ESTABLISHED : 연결되어 있는 상태
  - 그 외 교재 참조
> 07. TCP 재전송 기능
- TCP 전송 실패 인지시 재전송
  - 중복된 ACK 신호를 받았을 때
  - 전송 후 Timeout 이벤트가 발생했을 때
- TCP 오류 제어
  - ARQ(자동 재전송 요구)
    - 1. Stop-and-Wait ARQ : 요즘 안씀 => 해결 : 파이프라이닝
    - 2. Go-Back-N ARQ : 누적 확인 응답(Cumulative ARQ)
    - 3. Selectiv Repeat ARQ : 보낸 호스트에서 확인 응답 못 받은 세그먼트만 재전송
  - 빠른 재전송(fast retransmit)
    - 재전송 타이머가 만료되지 않아도 재전송
    - 3번의 중복 세그먼트 발생시
> 08. TCP의 혼잡 제어와 흐름 제어
- TCP 흐름 제어
  - 송신 버퍼와 수신 버퍼
  - 버퍼 오버플로우 문제 해결을 위해
  - 송신 호스트가 수신 호스트의 처리 속도를 고려 송수신 속도 균일하게
  - 슬라이딩 윈도우(sliding window)
    - 윈도우 : 파이프라이닝 가능한 순서번호 범위
- TCP 혼잡 제어(congestion)
  - 혼잡 윈도우 : 혼잡 없이 전송할 수 있을 법한 양
  - 주체 : 송신지
  - TCP 헤더에 포함되어 있지 않다
  - 기본 동작 형태 : AIMD(Addtive Increase Multicative Decrease) - 톱날형태
  - RTT(Round Trip Time, 왕복시간) : 메세지를 전송한 뒤 그에 대한 답변을 받는 시간
  - 알고리즘
    - 느린 시작 : 초기 전송 속도 확보를 위해 2배씩 증가
    - 혼잡 회피 : 느린 시작 임계치 도달 후 선형적인 속도(1 MSS) 증가
    - 빠른 회복 : 3번의 중복 ACK 세그먼트 수신시
    - TCP Reno(빠른 회복 수행)

### Ch.5 응용계층
> 01. DNS
- 도메인과 IP주소를 맵핑
- 계층적 도메인 구조 
  - Root DNS > TLD(Top-Level-Domain) >> n-th Level Domain
- Authoritative DNS : 찾고자 하는 도메인의 IP주소를 저장하는 최종 서버
- DNS Resolver : local DNS 서버
- 반복적 질의/재귀적 질의
- TTL : DNS 캐시를 설정 기간 동안 DNS에 저장
  
> 02. 자원과 자원의 식별
- 네트워크 상에서의 모든 '자원'
- URI(Uniform Resource Identifier)
  - 자원의 식별자
  - URL : 위치 기반 자원 식별(Locator)
  - URN : 이름 기반 자원 식별(Name)
- URL 구성 요소
  - scheme : 일반적인 프로토콜 이름 명시
  - authority = [userinfo "@"]host[":"port] => []은 생략 가능
  - path : 자원이 있는 경로
  - query : key:value 형태, '?' 쿼리의 시작, '&' 사용
  - fragment : HTML의 북마크 기능(특정 위치 식별)
