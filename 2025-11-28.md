# Python 기초 강의
- 강사 : 최우영
- Jupyter Notebook

## 1. Web Crawling
- css select로 찾는 방법 꼭 습득=>아주 많은 일을 할 수 있다.
- Searching Tree  
    - c.find_all(tagname): tag 이름으로 찾기(regular expression, list 가능)  
    - c.find_all(class=classname): 속성으로 찾기  
    - c.find_all(string=re.compile(‘대한민국’)): regular expression 
    - c.find_all(‘strong’, attrs={‘class’: ‘text’}) 
    - c.select(‘strong.text’): CSS selector로 찾기
    - c.find(tagname) == c.find_all(tagname, limit=1) 