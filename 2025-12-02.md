# AI 부트캠프 통계학
- 강사 : 김대한

## 1. Colab 한글 폰트 설치
!sudo apt-get update -y  
!sudo apt-get install -y fonts-nanum  

import matplotlib.pyplot as plt  
import matplotlib.font_manager as fm  

font_path = "/usr/share/fonts/truetype/nanum/NanumGothic.ttf"  

fm.fontManager.addfont(font_path)  
plt.rc('font', family='NanumGothic') # 폰트 설정 바꿈  

plt.rcParams['axes.unicode_minus'] = False # 한글이 깨지지 않도록 방지하는 부분  

print("> 한글 폰트 설정 완료")  

## 2. Boxplot 그리기

## 3. 통계 수치 다루기
- mean, median, mode(최빈값), Q1, Q3, IQR
- variance, std

## 4. Product, Permutations(순열), Combinations(조합)
- 합의 법칙
- 곱의 법칙
- 순열
- 조합

from itertools from product, permutations, combinations


