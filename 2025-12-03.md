# AI 부트캠프 통계학
- 강사 : 김대한


## 1. 확률 기초 & 확률과 인공지능
- 확률의 3가지 공리(Axioms)
  1) 범위
   - 0 <= P(A) <= 1
  2) 전체 확률
   - P(S) = 1
  3) 덧셈 법칙
   - A ⋂ B = ⌀ 이면,
   - P(A⋃B) = P(A) + P(B)
-  기본 성질
  - P(⌀) = 0
  - P(Ac) = 1 - P(A)
  - 단조성 : A가 B에 포함되면, P(A) <= P(B)

## 2. 조건부확률 P(A|B)와 독립
- 조건부확률의 핵심은 교집합(A⋂B)에 있다.
- 정의 : B가 **이미** 일어났다고 알고 있을 때, A가 일어날 확률
- 조건부확률은 "관점(분모)을 바꾸는 것"이다.
- P(A|B) = P(A⋂B)/P(B) (단, P(B) > 0)
- 독립 의 직관적 정의 : B가 일어났다는 사실이 A의 확률을 전혀 바꾸지 않을 때
  - 서로 영향을 주지 않는다.
  - 수식 정의 :
    - P(A⋂B) = P(A) P(B)
    - P(A|B) = P(A)

## 3. 베이즈 정리(Bayes' Theorem)
- 베이즈 정리 = P(결과|원인) -> P(원인|결과)로 뒤집기
- P(A) : 사전 확률(Prior) - 데이터 보기 전, A에 대한 믿음
- P(B|A) : 우도(Likelihood) - A일 때 B가 나올 가능성
- P(A|B) : 사후 확률(Posterior) - B를 본 뒤 업데이트된 믿음
- P(A|B) = P(B|A)P(A)/P(B)
- 의료검사 예시
  - P(A|B)
- P(A|B) = P(B|A)P(A)/{P(B|A)P(A) + P(B|Ac)P(Ac)}
  = (0.99x0.01)/{(0.99x0.01)+(0.05x0.99)}
  ~~ 0.167
- Page 325 채워보자 꼭~~
- Prior(사전) -> Evidence(증거) -> Posterior(사후)
- P(A) = 1% -> 양성(B) -> P(A|B) = 16.7%
- 처음에 스팸일지도 몰라 -> 제목이나 본문에 나타난 증거 -> 아, 이 메일은 스팸일 확률이 높아요!하고 업데이트 된 확률

## 4. 로지스틱 회귀
- 입력값을 계산해서 점수를 만든 뒤, 시그모이드 함수로 눌러서 확률로 만든다.
- 입력(x) -> 가중치 합(검수계산) -> 압축(σ) -> 확률(예측 결과)

- SoftMax

## 5. 
- A/B테스트 : 관측된 차이가 "우연"인지, "진짜"인지 판단하기 위해
  - 유의성(Significance) 과 p-value
  - p-value 가 높으면 우연, 낮으면 진짜 차이가 있다.
  - p < 0.05 => Statistically Significant
- 대수의 법칙 : 많이 던지면 수렴한다

## 6. 확률분포(Distribution)
- PMF : 이산(Discrete) 와 PDF : 연속(Continuous) 
- PMF : 확률질량함수
- PDF : 확률밀도함수 -> 곡선의 면적이 확률(적분)
- CDF(누적분포함수)

## 7. 이항분포(Binomial)
- 성공/실패 의 반복 상황을 이항분포로 설명
- n(시도횟수), p(성공확률)
- 베르누이 시행(Bernoulli trial) 
- Q3. 이항 분포가 아닌 경우는? (C)
A. 동전 10번 던지기(앞면 확률 0.5)에서 앞면 개수  
B.  클릭 확률이 매번 동일한 버튼을 100명의 유저에게 독립적으로 보여줄 때 클릭 수  
C. 유저가 여러 번 시도할수록 클릭 확률이 점점 증가하는 버튼의 클릭 수  
D. 주사위 20번 던져서 6이 나온 횟수  
E. 같은 종류의 부품을 독립적으로 30개 검사할 때 불량품 수  

## 8. 정규분포(Normal)
- 큰 n을 가진 이항분포는 정규분포를 가진다.
- 표준정규분포 : 평균 μ = 0, 분산 σ2 = 1 인 정규분포
- 1σ, 2σ, 3σ : 제조업 품질 검사, 성적 => +_3σ벗어나면 이상치라고 판단함
- 중심극한정리(CLT): 많은 작은 랜덤 요소들이 더해지면 -> 정규분포에 가까워진다.
- 정규분포 좋은 점
  - 확률 계산이 쉬워짐
  - 신뢰구간, t-test, Z-score, 이상치 판단 등 수많은 통계 도구를 바로 사용할 수 있다.

## 9. 통계적 추정(점추정, 구간추정)


## 10. 통계적 추정(Estimation)
- 모수(Parameter)와 통계량(Statics)
- 표본 
  - 평균 y-bar
  - 분산 s^2
- 표본분산 S^2 - 왜 n-1로 나눌까?
  - 불편추정량 (y-μ)^2 > (y-yˉ)^2 이기 때문에 그 차이 보정을 위해 과소 평가방지

## 11. 점추정(Point Estimation)
- 표본평균 yˉ로 모평균 μ 추정하기
- 추정량 표기 - "hat"기호(^)
- 좋은 추정량의 조건
  - 불편성(Unbiasedness) ; 불편추정량, 평균적으로 진짜 값을 맞춤
  - 일치성(Consistency)  : 데이터가 많을수록 오차가 0으로
  - (효율성(Efficiency) : 분산이 가장 작음)
=> 결론 : 표본평균 yˉ는 불편성과 일치성을 모두 갖춘 "좋은 추정량"이다.  
=> 불편성만 있으면 평균에 맞추지만 흔들림이 너무 큼  
=> 일치성만 있으면 데이터가 적을 때 엉뚱한 값을 줄 수 있음  

## 12. 신뢰구간(Confidencd Interval)
- 구간추정(Interval Estimation)과 함께 사용



## 실습과제.
- page 482, 483