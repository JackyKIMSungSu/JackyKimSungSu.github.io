# AI 부트캠프 리눅스 실전 정복

## 리눅스 운영파트

### Ch.2 리눅스 쉘과 CLI 명령어
#### 07. BASH 쉘과 친숙해지기
- 리눅스 쉘(Shell)이란?
  사용자 명령어 및 프로그램을 실행할 수 있는 공간(사용자 인터페이스)

- Bourne shell : $ 프롬프트
  - Bourn Again shell -> bash (요즘 많이 쓰는 쉘)
- C shell : % 프롬프트

- 프롬프트(prompt) 



- 환경변수 - 
  - echo $PATH
  - export PATH=$PATH:<추가할디렉토리>
  - which [filename] : 어디 실행파일이 실행되는지?
  - printenv : 다양한 환경변수 확인
  - LANGUAGE/LANG
    - LANGUAGE = en [명령어]
  - locale, localectl : 현재 로케일 정보 확인
- 단축명령어(alias)
  - ls = ls --color=auto 의 축약어
  - ll = ls -alF
  - alias ..="cd .."
  - alias ...="cd ../.."
  - alias en="LANGUAGE=en"
- 쉘 부팅(시작) 시퀀스(.profile, .bashrc 등)
  - /etc/profile 수행 -> /etc/profile.d/*.sh(공통 수행)-> /etc/bash.bashrc -> ~/profile -> ~/.bashrc -> ~/.bash_aliases(사용자 정의)
  - 종료 : ~/.bash_logout
- 쉘 스크립트
  - /bin/sh, /bin/bash 로 실행하거나
  - chmod +x test1.sh
  - ./test1.sh 처럼 직접 실행