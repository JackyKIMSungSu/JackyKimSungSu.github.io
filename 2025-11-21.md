# Python 기초 강의
- 강사 : 최우영

- Colab 코드 : https://colab.research.google.com/drive/1yly6f-tE6JLEK-EbcVC5p4qKY_JVnPjM#scrollTo=SMoPM8oKLdho
  
## 1. 연산자
## 2. 변수
> Computational Thinking
- 정답이 정해지지 않은 어떤 문제를 일반화하는 과정
- 문제상황 : 배가 고프다 -> (변수)을 먹는 행위로 배고픔을 해소
- 변수를 결정하는 다양한 파라미터
  - 날씨
  - 비가올 때 -> 전
  - 화창할 때 -> 냉면
  - 추울 때 -> 콩국수
  - 어제 안 먹은거
  - 시간
  - 건강
  - 기분 등등

> 예제
![alt text](images/image251121-01.png)

## 3. Import Python Enhance Proposal
### - _ : ignore, 이미 할당되어 있는 공간, 변수생성 필요없음 
> Naming Convention
- PascalCase : 클래스 이름
- snake_case : 함수, 변수, 메소드 이름
> Syntax
> operators
## 4. Floating point(부동소수점)
- round(x, y) 함수로 사용하여 소수점 자리 지정
- floor, ceil 사용시 from math import floor, ceil

## 5. input
- eval 함수 : 입력 혹은 할당받은 변수의 형을 정의. 메모리에 있는 변수타입 참조함
    > 예 :   
    result_3 = "THIS IS RESULT"  
    user_input = eval(input("Say something: "))  
    print(user_input, type(user_input))  
    -------------------------------------------
    > Say something: result_3  
    THIS IS RESULT <class 'str'>  

## 6. String formatting
- iterable
- immutable
  > 예  
    user_name = 'johndoe8511'  
    print('Hello, ', user_name)
    print('Hello, %s' % user_name)  
    print('Hello, {}'.format(user_name))  
    print(f'Hello, {user_name}') 
    -------------------------------------
  > Hello,  johndoe8511  
    Hello, johndoe8511  
    Hello, johndoe8511  
    Hello, johndoe8511  
- 문자열 정렬 포맷팅 ^, <, > 를 이용하여 크기 지정 및 정렬
  > 예 : length 30 크기로 고정 
    print(f'Hello, {s: ^30}. Today will be a great day!!')  
    print(f'Hello, {s: >30}. Today will be a great day!!')  
    print(f'Hello, {s: <30}. Today will be a great day!!')    
- split() 함수
  > 'C:/Windows/Desktop/dev/README.md'.split('/')[-1].split('.')[0]   
  'README'
- strip() 함수
  > '!!!!!!!!!!3.14!!!!!!!!!!'.strip('!')  
  '3.14'   
## 7. list 연산

## 8. membership operator : in

## 9. tuple : 고정형 리스트
- 한 번 선언한 자료를 추가, 수정, 삭제할 수 없습니다.

## 10. Dictionary, Set
> Dictionary
  - key, value로 구성
  - hash 이용 : 시간복잡도 = O(1)
  - cf. list의 시간 복잡도 = O(n)
  - list, tuple의 차이 : key 사용   
> Set
  - {}로 안에 값만 존재
  - 주용도 : 묶음자료형에서 중복을 제거해야 할 때 많이 쓰임
  
## 11. Conditional statement(조건문)

## 12. Ternary Operators in Python(삼항연산)
  > 예  
  > print("정답입니다." if guess == answer else "틀렸습니다.")
- 아주 간단한 비교에만 사용

## 13. Iteration(반복문)
- 반복문 제어 예약어
  - continue : 해당 사이클을 종료하고 다음 사이클로 넘어감
  - break : 반복문을 종료
  - continue와 pass 차이 : Colab 실행문 참고 (309,310줄)

