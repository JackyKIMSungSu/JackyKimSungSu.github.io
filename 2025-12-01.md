# AI 부트캠프 통계학
- 강사 : 김대한

## 로드맵
- Day 1 : 데이터 & 기초통계
- Day 2 : 확률 & AI
- Day 3 : 추정 & 프로젝트

## 감(Feeling) vs 데이터(Data)
- 편향된 소수 의견을 '전체'로 착각

- 배포 전 어떤 실험을 했어야 한다.

## 통계학의 세 가지 핵심 역할
- 데이터 요약
- 불확실성 정량화
- 의사결정 지원

## 데이터는 '전체'가 아니라 '표본'이다
- '표본(Sample)'을 보고 '모집단(Population)'을 추론하는 것
- '대표성 있는 표본'을 뽑는 것이 핵심

- 데이터를 읽는 힘, 왜곡하지 않는 힘

## 의사결정
- '괜찮다/아니다'가 아니라 수치화
- 근거 없는 느낌적 주장 보다는 근거있는 데이터 제시

## AI 모델 해석
- 통계없이 AI를 쓰면 '블랙박스(Black Box)'에 의존하게 된다.
- '정확도 95%'라는 숫자 뒤에 숨은 불확실성(오차)을 이해해야 한다.

## 현실 문제 vs 통계가 해주는 일
현실 : 통계적 대응
- 사용자 행동이 변했나? -> 기초통계량, 분포 비교
-
-
-

## 통계는 선택이 아니라 '기본언어'
- 데이터만으로는 부족하다 **'해석'** 이 필요함
- 감이 아니라 **'수치와 근거'** 의 도구
- AI 모델을 이해하는 **'필수 언어'**

# 2
## AI 개발 파이프라인
- 1.수집(Collection) -> 2.정제(Preprocessing) -> 3.EDA(Analysis) -> 4.특징(Feature Eng.) -> 5.학습(Training)-> 6.평가(Evaluation) -> 7.배포(Deploy)

## 어디에 통계가 숨어 있을까?
- 정제 : 이상치/결측치
- EDA : 기초통계/분포
- 평가 : 성능/신뢰도
- 모니터링 : 분포변화

## 1단계. 데이터 수집 - 무엇을, 누구에게서?
- 표본설계
- 표본크기
- 편향 위험 : 특정 집단 데이터만 모이면 결과가 왜곡된다.

## 2단계. 데이터 전처리 - 통계로 '청소'하기
- 결측치(Missing values) : 얼마나, 어디에 비어 있는가?
- 이상치(Qutliers) : 평균, 분산, 박스플롯으로 극단값 확인
- 기초통계량 확인 : min/max, 평균, 중앙값, 표준편차

## 3단계. EDA - 통계로 데이터의 얼굴을 본다
- 변수별 분포 : 히스토그램, 커널 밀도 그래프
- 요약 통계 : 평균, 중앙값, 사분위수, 표준편차
- 변수 간 관계 : 산점도(Scatter) 상관 계수
- **EDA = "모델 만들기 전에 데이터랑 친해지는 시간"**

## 4단계. 특징 엔지니어링 - 어떤 변수를 쓸까?
- 분산(Variance)이 0인 변수는 정보가 없다.(삭제)
- 타깃(정답)과 상관관계가 낮은 변수는 기여도가 낮다.
- 카이제곱 검정, 상관계수, 분산분석 등으로 **변수 중요도** 판단

카이제곱 검정->범주형 변수와 타깃 관계 판단  
상관계수 -> 연속형 변수와 타깃 관계  
분산분석 -> 특정 변수값에 따라 타깃이 유의미하게 달라지는지  

## 5단계. 모델 학습 - 통계적 관점에서 보기
- 많은 ML 알고리즘이 **통계 모델** 에서 출발했다
- 손실함수, 우도(likelihood), 최적화=**통계적 아이디어**
- 학습된 파라미터(Weight)도 결국 **'최적의 추정값'** 이다.

모델이 데이터를 가장 잘 설명하는 파라미터를 찾아가는 과정  

## 6단계. 모델 평가 - 통계가 가장 빛나는 순간
- 성능 지료 : 정확도, 정밀도, 재현율, F1, AUC
- 검증 방법 : Train/Test 분리, 교차검증(CV)
- A/B 비교 : 차이가 우연인지, 통계적으로 유의미한지

## 7단계. 배포&모니터링 - 시간이 지나도 괜찮을까?
- 데이터 드리프트(Drift)
  - 서비스 운영 중 데이터 분포가 바뀌는 현상
  - 예측 성능이 시간이 지나며 떨어지는지 확인 필요
  - 과거vs현재 분포 비교(히스토그램, KS테스트 등)

## 사례:광고 클릭률(CTR) 개선 프로젝트
1. 기존 CTR 데이터 수집(현황 파악)
2. 신규광고 문구 A/B 테스트 설계(실험 설계)
3. 각 버전의 CTR 평균,분포 비교(EDA)
4. 차이가 '우연이 아닐 확률' 검토(통계 검정)
5. 최적 버전 배포 및 모니터링(배포)

## 통계는 AI 팀의 공통 언어
- 기획자 : 이 실험이 의미가 있나요?
- 데이터 분석가 : 수치와 그래프로 설명
- ML 엔지니어 : 모델 튜닝과 평가 결과는..

AI를 잘 만들려면, 수학자까지는 아니어도 **'통게를 이해하는 개발자'** 는 되어야 한다.

## 척도의 종류



# 실습1.

# 실습2.
- 운동 강도 (1~10)
- 실무에서는 수치형(보고서)
- 이론에서는 범주형